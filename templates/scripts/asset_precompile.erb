#!/bin/bash
# Asset precompilation for <%= app_name %>
# Generated by Goico

set -e

echo "Precompiling assets for <%= app_name %>..."

cd <%= working_directory %>
export RAILS_ENV=production

<% if javascript_build %>
echo "Installing JavaScript dependencies..."
npm install --production
<% if build_command %>npm run build<% end %>
<% end %>

echo "Precompiling Rails assets..."
bundle exec rails assets:precompile

echo "Assets precompiled successfully"