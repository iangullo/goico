# Scheduled jobs for <%= app_name %>
# Generated by Goico

# Clear expired sessions
0 2 * * * <%= user %> cd <%= app_install_path %> && bundle exec rails db:sessions:clear

<% if @analysis[:infrastructure][:background_processor] == :sidekiq %>
# Sidekiq monitoring
* * * * * <%= user %> cd <%= app_install_path %> && bundle exec sidekiqmon
<% end %>

<% if @options[:backup_schedule] %>
# Database backups
<%= @options[:backup_schedule] %> <%= user %> <%= app_install_path %>/config/deploy/backup.sh
<% end %>